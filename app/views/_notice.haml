- random_tips current_user
- [:error, :success, :info].each do |tag|
  - if session[tag]
    %div(class="alert alert-#{tag}")
      %button(class='close' data-dismiss='alert')×
      = session.delete(tag)
